import {grey} from "@mui/material/colors";

const toolbarStyles = {
    root: {
        padding: "8px",
        borderBottom: `2px solid ${grey[300]}`,
        overflow: "auto",
    }
};

export default toolbarStyles;